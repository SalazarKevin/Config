1)dejar el método como "áreas", no como 2 dedos

2)agregar /etc/modprobe.d/blacklist.conf
  # remove SynPS/2 Synaptics Touchpad because we want the mouse to work over IC2b
  blacklist psmouse

3)sudo update-initramfs -u
4)sudo reboot

5) synclient TapButton2=2 TapButton3=3 HorizEdgeScroll=1 HorizTwoFingerScroll=0, colocarlo como aplicación de inicio


